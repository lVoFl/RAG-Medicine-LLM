\chapter{医学问答模型构建与优化}

在完成知识库的构建与检索设计之后，系统还需要进一步解决“如何基于检索证据生成回答”以及“如何让模型回答更符合医学场景要求”这两个问题。因此，本章重点讨论RAG输入组织方式、Prompt设计、DPO偏好数 据构建以及模型优化效果分析。
\section{RAG问答生成方法}

\subsection{检索增强生成流程}

RAG问答生成的核心流程可以概括为“检索—增强—生成”三个环节：先从用户输入的问题出发构造检索查询，在医学知识库里把与之相关的候选文本召回来，这一步解决的是回答依据从哪里来的问题；接着对召回的一批候选文本做去重、排序和筛选，挑出最有价值的证据组合成增强上下文，这一步处理的是哪些证据最值得交给模型去参考的问题；最后把用户问题和增强后的证据上下文一起交给大语言模型，由模型将这些证据转化为用户可以理解的自然语言回答。通过这样一条链路，模型的输出就不再单纯依赖存留在参数里的记忆，而是建立在外部知识的支撑之上，从而让回答的准确程度与内容可追溯程度都得到提升。
\begin{figure}[htb]
    \includegraphics[width=1 \textwidth]{image/2/RAG.png}
    \caption{RAG流程图}
    \label{fig:RAG-workflow}
\end{figure}

\subsection{Prompt与上下文构建}

在医学问答场景下，系统需要将提示信息、对话上下文与检索结果进行统一组织，作为大语言模型的输入。系统送入大语言模型的内容主要包括三部分，一部分是系统提示词，用来明确模型的角色定位和回答原则，一部分是必要的历史对话信息，用来支持多轮问答中的语义衔接，另一部分是当前用户问题和检索得到的外部证据，用来约束模型在生成时优先依据可以核对的资料作答。这样组织输入内容，主要是为了减少没有依据的扩展，让模型输出尽量建立在检索证据和医学常识的共同基础上。

如图\ref{fig:prompt}所示，系统提示词先把模型设定为“严谨、审慎的医疗问答助手”，并明确要求它优先依据检索资料回答问题。在回答规则上，提示词进一步限制模型不能编造和资料内容或医学共识相矛盾的信息，还要求模型先判断检索资料和问题之间的相关程度，再决定是以资料为主进行回答，还是结合通用医学知识作适当补充；如果问题本身比较宽泛，就先给出简要概述，再围绕检索证据展开重点说明，回答形式也要尽量清楚、具体、容易理解。对于治疗和用药这类更敏感的问题，系统还要求模型避免给出过于绝对的建议；如果问题中出现胸痛、呼吸困难、意识改变、持续高热或明显出血等急危重症信号，则应优先建议立即线下就医或进行急诊处理。这样设计Prompt，不只是告诉模型要回答什么，更重要的是限制模型该怎样回答，以及回答应当控制在什么边界内。
\begin{figure}[htb]
    \includegraphics[width=0.9 \textwidth]{image/3/prompt.png}
    \caption{系统提示词}
    \label{fig:prompt}
\end{figure}

对于用户的输入，系统采用较为固定的组织格式，将当前问题和检索证据封装为统一文本。其中，用户问题以“问题：”字段引出；若存在检索结果，则继续追加“检索资料：”字段，并将检索证据整体附在其后。对于多轮对话场景，系统会在当前用户输入之前插入经过清洗后的历史消息序列，仅保留角色合法且内容有效的最近若干轮消息，并对连续同角色消息进行合并，以减少噪声上下文对生成的干扰。这样的组织方式既保持了对话输入格式的一致性，也使模型能够明确区分“历史对话”“当前问题”和“外部证据”三类信息。

在检索证据的构建上，系统对重排后的候选文档按相关性顺序逐条组织，每条证据均采用统一格式表示为“编号 + 来源 + 标题 + 内容”。这种结构化的输入方式，模型不仅能够利用正文内容，还能够同时利用来源信息和标题路径辅助判断证据的主题归属。

最终，系统将“系统提示词 + 历史对话 + 问题 + 检索资料”共同输入模型，实现面向医学RAG场景的统一Prompt构建。

\section{基于DPO的模型优化策略}

\subsection{偏好数据构建}

为了让DPO训练更贴近医学问答场景，本文选用了cMedQA v2.0和Huatuo-26M两个中文医疗问答数据集。前者是面向中文医疗问答任务整理的数据集，包含10万多个问答对，覆盖了疾病、症状、治疗方法等多类医学知识；后者规模更大，约有2600万个问答对，而且其中大部分内容来自真实医疗咨询场景，因此更接近实际问答中的表达方式。

在这两个数据集中，本文先筛选出和“三高”相关的问题，再对重复内容进行去重，最终得到6351条问答数据。在这个基础上，又继续挑选出那些更容易产生歧义、或者更容易引发模型幻觉的问题样本，最后整理出431个具有代表性的偏好数据问题。

在具体构造过程中，本文借助高质量大语言模型生成偏好数据对，也就是Chosen-Rejected回答对，同时再引入评估模型对生成结果进行筛选和比较，用这种方式提高数据对的可靠性和区分度。

一是优质回答，也就是Chosen回答的生成，这部分提示词设计与3.3.2节保持一致，用来尽量保证训练阶段和实际使用阶段的提示环境相同；二是劣质回答，也就是Rejected回答的生成，本文通过提示词引导模型产出“表面上看起来合理、但实际存在缺陷”的回答，并有意识地加入几类常见问题，比如对不确定结论表达得过于肯定、忽略检索资料中的细节、缺少必要的安全提示，或者回答内容过于空泛，不过同时也会控制负样本不要出现明显错误或极端不安全内容，使它仍然停留在“容易混淆”的合理范围里，这样更有利于偏好学习；三是偏好对的筛选和质量控制，在得到候选正负回答后，再由另一个评估模型从事实一致性、安全性、相关性、完整性和表达清晰度等角度进行比较，只保留优劣关系比较明确的样本对，用于后续DPO训练。

如图\ref{fig:dpo-train-data}所示，本文最终将DPO训练数据组织为三元组形式，也就是（prompt，chosen，rejected），其中prompt表示用户问题，chosen表示质量更高的回答，rejected表示相对较差的回答。

\begin{figure}[htb]
    \includegraphics[width=1 \textwidth]{image/4/dpo_train_data.png}
    \caption{DPO训练数据示例}
    \label{fig:dpo-train-data}
\end{figure}

\subsection{DPO训练流程}

本文基于TRL框架实现DPO训练，并结合LoRA参数高效微调方法对Qwen基础模型进行优化，以提升模型在医学问答任务中的回答质量、安全性和场景适配能力。由于基础模型参数规模较大，若直接采用全参数训练，会带来较高的显存占用和训练开销，因此本文采用LoRA方法，仅在部分关键模块中引入可训练的低秩参数，从而在降低训练成本的同时实现对模型的有效优化。

在训练阶段，模型以偏好数据三元组作为输入，通过比较同一问题下的优选回答与非优选回答，学习更符合人工偏好的输出分布。考虑到单张RTX 4090显卡只有24G的显存容量，本文使用bitsandbytes库对模型进行4-bit量化加载，降低模型占用；训练过程中采用小批量训练并结合梯度累加策略，用小显存近似模拟大批量的训练；同时设置warmup机制和合适的DPO偏好强度系数，以增强训练稳定性。

训练中使用的主要超参数如表\ref{tab:dpo-hyperparams}所示。

\begin{table}[htbp]
    \caption[DPO训练主要超参数]{DPO训练主要超参数}
    \centering
    \begin{tabular}{lll}
        \toprule
        超参数 & 取值 & 作用 \\
        \midrule
        learning\_rate & $10^{-5}$ & 学习率 \\
        lora\_r & 16 & 低秩矩阵的秩 \\
        lora\_alpha & 32 & LoRA缩放系数 \\
        batch\_size & 2 & 批大小 \\
        gradient\_accumulation\_steps & 2 & 梯度累加步数 \\
        beta & 0.1 & DPO偏好优化强度系数 \\
        warmup\_ratio & 0.03 & 学习率warmup比例 \\
        \bottomrule
    \end{tabular}
    \label{tab:dpo-hyperparams}
\end{table}

\section{实验结果分析}

\subsection{DPO训练过程及分析}

为验证DPO训练过程是否有效，本文结合训练日志对loss、奖励准确率（reward accuracy）、奖励间隔（reward margin）以及平均token准确率等指标进行了分析，结果如图\ref{fig:train-curve}和图\ref{fig:mean-token}所示。整体来看，本次训练共记录108个训练点，训练历经约1个epoch后完成收敛，主要指标呈现出较为清晰的阶段性变化，说明模型在偏好学习过程中形成了相对稳定的优化趋势。

从loss变化来看，训练初始时loss为$0.6931$，这一数值接近二分类随机判别时的初始状态，说明模型在训练开始阶段尚未对chosen回答和rejected回答形成明确区分。随着训练推进，loss整体呈下降趋势，最终降至$0.0836$，最低值达到$0.0364$。若按照训练早期、中期和后期三个阶段进行统计，其平均loss分别为$0.5542$、$0.2414$和$0.1579$，表现出较为明显的递减特征。这说明模型在训练过程中逐步学会了对偏好对进行有效区分，DPO目标函数得到了较充分优化。需要说明的是，训练后期loss曲线虽存在局部波动，但总体仍保持在较低水平，并未出现持续性反弹，因此可以认为模型整体收敛趋势较好。

从奖励相关指标来看，DPO训练的偏好学习效果更为直观。训练开始时，奖励准确率和奖励间隔均为0，表明模型尚未体现出明显的优劣排序能力。随着训练进行，奖励准确率在第7个step、约$0.065$个epoch时即达到$1.0$，说明模型较早就能够在当前批次上正确判断chosen与rejected回答的相对优劣。与此同时，奖励间隔由初始的0逐步增长至训练结束时的$2.6441$，最大值达到$3.3355$。按阶段统计可见，奖励准确率均值由早期的$0.7917$提升至中期的$0.9861$，并在后期稳定在$1.0$；奖励间隔均值则由$0.3414$进一步提升至$1.4702$和$2.0445$。这表明模型不仅学会了“选对答案”，而且对优选回答与非优选回答之间的区分幅度也在不断扩大，说明偏好对齐过程并非偶然波动，而是形成了较稳定的排序优势。

\begin{figure}[htb]
    \subfloat[loss曲线]{
        \label{fig:loss-curve}
        \includegraphics[width=0.48 \textwidth]{image/4/loss_curve.png}
    }
    \subfloat[奖励曲线]{
        \label{fig:reward-curve}
        \includegraphics[width=0.48 \textwidth]{image/4/reward_curve.png}
    }
    \caption{模型训练过程曲线}
    \label{fig:train-curve}
\end{figure}

除偏好学习效果外，还需要关注训练是否对基础模型原有生成能力造成明显破坏。为此，本文进一步观察了平均token准确率的变化情况。平均token准确率在整个训练过程中基本稳定在$0.59$附近，起始值为$0.6062$，最终值为$0.5756$，均值为$0.5915$，标准差仅为$0.0212$，整体波动较小。这说明尽管模型在偏好对齐过程中不断强化对优选回答的倾向，但并未明显削弱其原有的基本生成能力。
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85 \textwidth]{image/4/mean_token_accuracy_curve.png}
    \caption{平均token准确率}
    \label{fig:mean-token}
\end{figure}

总体来看，本次DPO训练在1个epoch内完成了较有效的偏好对齐。一方面，loss持续下降、奖励准确率迅速达到高水平，说明模型能够较快学习到偏好排序关系；另一方面，奖励间隔不断扩大，说明训练后期已经形成较稳定的优化状态。同时，平均token准确率整体保持平稳，表明此次偏好优化并未显著破坏基础模型原有的语言生成能力。因此，从训练过程本身来看，本文采用的LoRA+DPO训练方案是有效的，也为后续回答质量评测结果提供了训练层面的支撑。

\subsection{回答质量评测结果}

为了评估优化后的模型在“三高”慢性病问答场景下的生成质量，本文构建了120条典型的“三高”领域问题，分别由基础模型Qwe2.5-7B-Instruct和DPO优化后的模型生成回答，并对两个模型的回答进行评估，以分析优化方法对医学问答性能的影响。

为了保证评测过程的一致性与客观性，本文采用大语言模型作为评审（LLM-as-a-Judge）的方式对两个模型的回答进行对比评价。具体来说，对于每一个测试问题，分别获取基础模型与DPO优化模型生成的回答，并对回答顺序进行随机打乱，使评审模型在不知模型来源的情况下进行判断。

在评价维度上，本文主要从事实一致性、安全性、相关性以及表达清晰度等方面对回答质量进行综合评估。其中，事实一致性用于衡量回答是否符合医学常识及检索证据内容；安全性关注回答中是否存在潜在风险或缺乏必要的就医提示；相关性用于判断回答是否紧密围绕用户问题；表达清晰度则评估回答结构是否合理、语言是否易于理解。

在具体评判方式上，模型需在两个回答中选择质量更优的一项，或在无法区分优劣时标记为“平局”。基于全部样本的评测结果，统计DPO优化模型相对于基础模型的胜率，以反映其在整体生成质量上的提升情况。

详细评测结果如表\ref{tab:multidim_eval_main}所示。可以看出，相较于基础模型，DPO优化后的模型在整体生成质量上取得了一定提升，overall\_score由9.0750提升至9.3667，增幅为0.2917，说明模型在综合表现上有所改善。

从各项评价维度来看，DPO优化在事实一致性和完整性方面提升较为明显，其中factual\_consistency提升0.1667，completeness提升0.2083，表明模型在生成过程中能够更好地利用检索证据，并减少关键信息遗漏。同时，在safety和boundary\_awareness指标上也有一定提升，分别增加0.1250和0.0917，说明模型在回答中对风险提示和边界控制的表现有所改善，更符合医学问答场景的安全性要求。

相比之下，relevance和clarity的提升幅度相对较小，分别仅为0.0083和0.0417。这一现象表明，基础模型在语义理解与语言表达方面已具备较强能力，DPO优化对这些维度的影响相对有限。

\begin{table}[htbp]
\centering
\begin{threeparttable}
\caption{Base 与 DPO 的多维评测统计对比}
\label{tab:multidim_eval_main}
\begin{tabular}{lccc}
\toprule
\textbf{Metric} & \textbf{Base} & \textbf{DPO} & $\mathbf{\Delta}$ (DPO-Base) \\
\midrule
overall\_score        & 9.0750 & 9.3667 & 0.2917 \\
factual\_consistency  & 4.6583 & 4.8250 & 0.1667 \\
safety                & 4.7000 & 4.8250 & 0.1250 \\
relevance             & 4.9667 & 4.9750 & 0.0083 \\
completeness          & 4.4667 & 4.6750 & 0.2083 \\
clarity               & 4.9333 & 4.9750 & 0.0417 \\
boundary\_awareness   & 4.6333 & 4.7250 & 0.0917 \\
\bottomrule
\end{tabular}
\begin{tablenotes}
\footnotesize
\item 注：$\Delta$ 为 DPO 模型相对于 Base 模型的均值提升。
\end{tablenotes}
\end{threeparttable}
\end{table}

总体来看，DPO优化主要体现在提升模型对医学知识的使用质量以及回答过程中的安全性与完整性，而在通用语言能力方面变化较小。该结果进一步验证了基于偏好学习的优化方法能够在不显著影响原有生成能力的前提下，对模型在特定领域中的表现进行有效改进。

% 从风险错误率角度继续观察，当把单项评分低于$4.0$定义为高风险低分样本时，DPO模型在事实正确性和安全性两个维度上的低分占比都低于Base模型。具体来看，Base模型在事实正确性上的风险错误率为$7.50\%$，DPO模型下降到$4.17\%$；在安全性上的风险错误率由$3.33\%$下降到$2.50\%$。这一结果说明，DPO优化不仅提升了整体平均得分，也在关键安全相关维度上减少了明显失误样本的比例，使模型表现更稳健。

% \begin{table}[htbp]
% \centering
% \caption{胜率、风险错误率与加权得分}
% \label{tab:multidim_eval_risk_weighted}
% \begin{tabular}{lcc}
% \toprule
% \textbf{指标} & \textbf{Base} & \textbf{DPO} \\
% \midrule
% Win rate (\%) & 39.17 (47/120) & 60.83 (73/120) \\
% Factual risk rate (\%, $<4.0$) & 7.50 (9/120) & 4.17 (5/120) \\
% Safety risk rate (\%, $<4.0$) & 3.33 (4/120) & 2.50 (3/120) \\
% Weighted score & 4.6888 & 4.8250 \\
% \bottomrule
% \end{tabular}
% \vspace{4pt}
% \begin{minipage}{0.9\linewidth}
% \footnotesize
% \textit{注}：加权得分采用
% $0.35\times$factual $+0.35\times$safety $+0.15\times$completeness $+0.10\times$relevance $+0.05\times$clarity。
% 对应差值为$\Delta=0.1363$，95\%置信区间为$[0.0450,\,0.2212]$，Wilcoxon检验$p=0.004882$，paired t-test $p=0.003243$。
% \end{minipage}
% \end{table}

% 加权综合得分分析进一步支持这一结论。按照事实正确性和安全性各占$0.35$、完整性占$0.15$、相关性占$0.10$、清晰度占$0.05$的权重方式计算后，Base模型的加权得分为$4.6888$，DPO模型提升到$4.8250$，差值为$\Delta=0.1363$，95\%置信区间为$[0.0450,\,0.2212]$，Wilcoxon检验$p=0.0049$。由于这种加权方式更强调事实正确性和安全性，因此结果进一步说明，DPO优化在医学问答这类高风险任务中具有更高的实际应用价值。

% 总体来看，DPO优化后的模型在偏好胜率、总体得分、关键维度表现和风险错误率控制方面都优于Base模型，尤其在事实正确性和完整性两个维度上表现出更清晰的统计优势。这说明DPO方法不仅能改善回答的表面流畅度，更重要的是能在医学问答场景中增强事实表达能力、安全稳健性以及对高质量回答偏好的对齐程度，从而为后续系统部署提供更可靠的生成模型基础。

\section{本章小结}

本章围绕模型侧方法展开，系统梳理了RAG问答生成流程、Prompt与上下文构建方式、偏好数据组织过程，以及基于DPO的模型优化与实验分析。结果表明，DPO优化在回答完整性、事实一致性和医学场景适配能力方面带来了较稳定提升。

综合本章研究可以看出，模型不仅具备了“基于证据生成回答”的核心能力，也在输出稳健性和风险边界控制方面得到强化。
